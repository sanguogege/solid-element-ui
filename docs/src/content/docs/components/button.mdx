---
title: Button 组件
description: 按钮组件的使用说明。
---

import { SeButton } from "solid-element-ui";

# 这是一个按钮示例

这是从你的库中引入的按钮：

<SeButton client:load>点击我</SeButton>

{/* 如果只是展示 UI 无交互，可以不加 client 指令，但通常建议加上以保证功能正常 */}
