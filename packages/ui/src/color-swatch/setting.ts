import { tv } from "tailwind-variants";

export const colorSwatchVariants = tv({
    base: "h-8 w-8 rounded-md border border-zinc-200 shadow-sm transition-transform hover:scale-105 dark:border-zinc-800",
});
