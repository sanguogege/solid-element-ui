import { tv } from "tailwind-variants";

export const linkVariants = tv({
    base: "text-sm font-medium text-zinc-950 underline-offset-4 hover:underline data-[disabled]:pointer-events-none data-[disabled]:opacity-50 dark:text-zinc-50",
});
