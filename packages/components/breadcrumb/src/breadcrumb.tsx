import { type ParentComponent } from "solid-js";
import { type BreadcrumbProps } from "./setting";

export const SeBreadcrumb: ParentComponent<BreadcrumbProps> = (props:BreadcrumbProps) => {
    return (
        <div>SeBreadcrumb</div>
    );
};
